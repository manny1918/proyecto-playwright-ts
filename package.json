{
  "name": "proyecto-playwright-ts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "run-test": "npx playwright test --reporter=line,allure-playwright --headed",
    "generate-report": "allure generate ./allure-results -o ./allure-report  --clean",
    "open-report": "allure open ./allure-report",
    "test": "npm run run-test ; npm run generate-report && npm run open-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.47.1",
    "@types/node": "^22.5.5",
    "allure-playwright": "^3.0.0"
  }
}
